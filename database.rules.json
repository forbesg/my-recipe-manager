{
  "rules": {
    ".read": false,
    ".write": false,
    "recipes": {
      ".read": true,
      ".write": "auth != null && !data.exists()",
      "$recipe_id": {
        ".write": "auth != null && !data.exists() || auth != null && root.child('recipes/'+$recipe_id+'/owner/uid').val() === auth.uid",
      	"favoritedBy": {
          ".write": "auth != null"
        }
      }
    }
  }
}
